const soma = 2 + 4;
const diff = 10 - 5;
const multiply = 5 * 10;
const division = 40 / 5;
const module = 5 % 2;
const power = Math.pow(5, 3);
const squareRoot = Math.sqrt(125);

console.log(soma, diff, multiply, division, module, power, squareRoot);